# Student Exam Predictor 🎓

This is a machine learning web app built with Streamlit that predicts whether a student will pass an exam based on:

- Study Hours
- Attendance %
- Previous Scores

## How to Run
1. Install requirements: `pip install -r requirements.txt`
2. Run the app: `streamlit run app.py`
